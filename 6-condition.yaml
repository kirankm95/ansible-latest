- name: first play
  become: yes
  hosts: localhost
  tasks:
    - name: key value pair1
      ansible.builtin.command: id roboshop
      register: user
      ignore_errors: true

    - name: 222 key value pair22222
      ansible.builtin.debug: 
        msg: "{{user.rc}}"

    - name: 222 key value pair22222345
      ansible.builtin.user: 
        name: roboshop
      when: user.rc != 0